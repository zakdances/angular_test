import { TableFormatterPipe } from './table-formatter.pipe';

describe('TableFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new TableFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
